<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AletheaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alethea">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Edgar Rosario -->
        <activity android:name=".EdgarRosario.InicioActivity" />
        <activity android:name=".EdgarRosario.IniciarSesionActivity" />
        <activity android:name=".EdgarRosario.BienvenidoUserActivity" />
        <activity android:name=".EdgarRosario.BienvenidoAdminActivity" />
        <activity android:name=".EdgarRosario.RegistrarseActivity" />

        <!-- Carter Kenneth -->
        <activity android:name=".CarterKenneth.FavoritosActivity" />
        <activity android:name=".CarterKenneth.DetalleLibroActivity" />
        <activity android:name=".CarterKenneth.CatalogoLibrosActivity" />
        <activity android:name=".CarterKenneth.MisPrestamosActivity" />
        <activity android:name=".CarterKenneth.PerfilUsuarioActivity" />

        <!-- Mejia Rachel -->
        <activity android:name=".MejiaRachel.ReporteEstadisticaActivity" />
        <activity android:name=".MejiaRachel.PrestamosDevolucionesActivity" />
        <activity android:name=".MejiaRachel.GestionLibrosActivity" />
        <activity android:name=".MejiaRachel.EditarLibroActivity" />

        <!-- Jhezrrel Delgado -->
        <activity android:name=".DelgadoJhezrrel.AgregarLibrosActivity" />
        <activity android:name=".DelgadoJhezrrel.AjustesActivity" />
        <activity android:name=".DelgadoJhezrrel.GestionAutoresActivity" />
        <activity android:name=".DelgadoJhezrrel.GestionUsuariosActivity" />
    </application>

</manifest>
